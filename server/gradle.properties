# Gradle 타임아웃 설정 (FFmpeg 대용량 라이브러리 다운로드용)
org.gradle.daemon.idletimeout=10800000
systemProp.http.socketTimeout=600000
systemProp.http.connectionTimeout=600000

# FFmpeg 플랫폼별 다운로드 최적화 (Docker 빌드 시 linux-x86_64만 필요)
org.bytedeco.javacpp.platform=linux-x86_64

